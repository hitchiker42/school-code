#include "vec_math.h"
#include <math.h>
#include <stdio.h>
//add vectors x and y of size n and store the result in z
double* vec_add(double *x,double *y,double *z,int n){
  //double z[n];//check syntax
  for (int i=0;i<n;i++){
    z[i]=0;
    z[i]=x[i]+y[i];
  }
  return z;
}
